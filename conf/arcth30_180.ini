# config ini

[run]
dryrun = False

# leave it blank if you want the timelabel
session_label = arcth30_180
movetable =  movetables/arcth30_180cap.txt 
mounttable = mounttables/DownHorizonUp.txt
repeatcycle = 10

description = theta arcs 30 deg with open phi arm over a 180 degree range

# steps to run
pipeline = positioners cam 
# e.g. pipeline = mount positioners cam spotfinder

#todo
verbose=False


rootout = /home/msdos/DESI-QA/sbigpics/  
pbroot = /home/msdos/pclight/trenz
